// Next.js's built-in `*.module.css` declaration doesn't cover plain CSS
// side-effect imports (e.g. `import "./globals.css"`). Declaring it here
// satisfies the TypeScript language server.
declare module "*.css";
