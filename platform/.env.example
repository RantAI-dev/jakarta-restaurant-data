# Postgres (lokal via Docker, atau Neon di prod)
DATABASE_URL=postgres://user:pass@host/db?sslmode=require

# Secret untuk endpoint /api/admin/sync (header x-sync-secret)
SYNC_SECRET=ganti-dengan-string-acak-panjang
